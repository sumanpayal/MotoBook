import { ImageStyle, TextStyle, ViewStyle } from 'react-native'

type NoRecordFoundProps = {
	noRecordText?: string
	containerStyles?: ViewStyle
	noRecordTextStyles?: TextStyle
	isImage?: boolean
	imageSource?: any
	imageStyle?: ImageStyle
	isLottieImage?: boolean
	LottieImage?: any
}
